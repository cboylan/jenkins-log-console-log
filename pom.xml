<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>2.3</version><!-- which version of Jenkins is this plugin built against? -->
    </parent>

    <groupId>hudson.plugins.ConsoleLogToWorkspace</groupId>
    <artifactId>console-log-to-workspace</artifactId>
    <version>1.2-SNAPSHOT</version>
    <packaging>hpi</packaging>

    <name>Console Log To Workspace</name>
    <description>Write the console log to a file in the workspace</description>

    <licenses>
        <license>
            <name>Apache license</name>
            <url>https://opensource.org/licenses/Apache-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <jenkins.version>2.3</jenkins.version>
        <java.level>7</java.level>
        <jenkins-test-harness.version>2.19</jenkins-test-harness.version>
    </properties>

    <!-- get every artifact through repo.jenkins-ci.org, which proxies all the artifacts that we need -->
    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

</project>
